<Window x:Class="Proekt3.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Proekt3"
        mc:Ignorable="d"
        Title="LoginWindow" Height="400" Width="350"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <!-- Разметка строк окна -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок окна -->
        <TextBlock Text="Вход в систему" FontSize="20" FontWeight="Bold" 
                   Foreground="{StaticResource PrimaryColor}" 
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <!-- Поле ввода логина -->
        <StackPanel Grid.Row="1" Margin="0,10">
            <TextBlock Text="Логин" Margin="0,0,0,5"/>
            <TextBox x:Name="txtLogin" FontSize="14" Padding="8"/>
        </StackPanel>

        <!-- Поле ввода пароля -->
        <StackPanel Grid.Row="2" Margin="0,10">
            <TextBlock Text="Пароль" Margin="0,0,0,5"/>
            <PasswordBox x:Name="txtPassword" FontSize="14" Padding="8"/>
        </StackPanel>

        <!-- Кнопки Вход и Выход -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20">
            <Button x:Name="btnLogin" Content="Вход" Style="{StaticResource ButtonStyle}" 
                    Width="100" Click="BtnLogin_Click"/>
            <Button x:Name="btnExit" Content="Выход" Style="{StaticResource ButtonStyle}" 
                    Width="100" Click="BtnExit_Click"/>
        </StackPanel>

        <!-- Сообщения об ошибке -->
        <TextBlock x:Name="txtError" Grid.Row="4" Text="" Foreground="{StaticResource ErrorColor}"
                   HorizontalAlignment="Center" VerticalAlignment="Bottom" FontSize="12"/>
    </Grid>
</Window>
